// OPERADOR TERNÁRIO - <condição> ? <verdadeiro> : <falso>
let nota = 9;
let media = 5;
let faltas = 10;
let faltasMaximas = 10;

let resultado = nota >= media && faltas <= faltasMaximas ? 'Verdadeiro' : 'Falso'

console.log(resultado)